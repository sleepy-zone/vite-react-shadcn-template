# React + TypeScript + Vite + shacdn ui + Tailwind CSS

```bash
npm install -g yarn 
yarn 
yarn dev

# http://localhost:5173/
```

if you want install a new shacdn component:
```bash
npx shadcn-ui@latest add alert
```